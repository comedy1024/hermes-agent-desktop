# ModelScope 创空间 Dockerfile
# 使用 hermes-agent-desktop 镜像

FROM ghcr.io/comedy1024/hermes-agent-desktop:latest

# 桌面分辨率（云平台推荐较小分辨率以减少资源消耗）
ENV RESOLUTION=1024x768

# 不需要 COPY、RUN 或 ENTRYPOINT
# 基础镜像的 /entrypoint.sh 会自动启动 VNC + KDE + Hermes
